# {{- if or (eq .env "stage") (eq .env "prod") (eq .env "euprod") }}
# ---
# image: {{ .project }}-pg-{{ .env }}
# from: postgres:14
# ansible:
#   beforeInstall:
#   - name: Disable docker hook for apt-cache deletion
#     shell: |
#       set -e
#       sed -i -e "s/DPkg::Post-Invoke.*//" /etc/apt/apt.conf.d/docker-clean
#       sed -i -e "s/APT::Update::Post-Invoke.*//" /etc/apt/apt.conf.d/docker-clean
#   - name: Install connector plugin
#     shell: |
#       apt update
#       apt-get install -y postgresql-14-wal2json wget
#       sed -i -e"s/^#wal_level =.*$/wal_level = logical/" /usr/share/postgresql/postgresql.conf.sample
#       sed -i -e"s/^#max_replication_slots =.*$/max_replication_slots = 10/" /usr/share/postgresql/postgresql.conf.sample
#       sed -i -e"s/^#max_wal_senders =.*$/max_wal_senders = 10/" /usr/share/postgresql/postgresql.conf.sample
